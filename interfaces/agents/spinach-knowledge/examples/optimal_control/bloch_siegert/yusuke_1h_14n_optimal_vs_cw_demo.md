# examples/optimal_control/bloch_siegert/yusuke_1h_14n_optimal_vs_cw_demo.m

- Source: `/home/kuprov/.openclaw/workspace/Spinach/examples/optimal_control/bloch_siegert/yusuke_1h_14n_optimal_vs_cw_demo.m`
- Signature: `yusuke_1h_14n_optimal_vs_cw_demo()`
- Total lines: 264

## Purpose

Reduced heteronuclear control demonstration inspired by the low-power offset-tolerant 14N decoupling papers of Nehra, Agarwal, and Nishiyama. The model uses an observed 1H spin coupled to a controlled 14N spin: -controls act on 14N only, -the optimisation target is preservation of 1H magnetisation, -14N offset and B1 distributions represent the practical decoupling problem, -the Bloch-Siegert term is generated by, an

## Physical / mathematical content

- Optimal-control examples. These scripts formulate pulse design as a nonlinear optimisation problem over waveform samples or basis coefficients. The core mathematical objects are fidelities, gradients, Hessians or Hessian approximations, ensemble robustness objectives, and constrained search over RF amplitude/phase trajectories.
- The numerical method is quasi-Newton optimisation: curvature information is approximated from successive step and gradient differences instead of forming exact second derivatives every iteration.
- The numerical method is limited-memory quasi-Newton optimisation, which keeps only a short curvature history and is therefore suitable for waveform vectors too large for dense Hessians.
- The optimisation logic is Newton or Newton-like: search directions use first- and second-order local curvature information, usually with regularisation or line-search safeguards.
- The control theory content is GRAPE: fidelity derivatives are propagated through a piecewise-constant pulse sequence so that waveform samples can be improved by gradient-based optimisation.

## Numerical / algorithmic content

- The file is built around the standard Spinach workflow: create the spin system, choose a basis or context, assemble operators/superoperators, then propagate or analyse the resulting dynamics.
- The file also defines local helper function(s): `offset_profile()`, `b1_profile()`, `ensemble_score()`, `single_score()`. This usually means the public entry point is supported by tightly coupled validation or helper logic kept private to the file.

## Code-derived implementation details

### Comment-guided execution stages

- Lines 25-26: Magnetic field corresponding to 800 MHz 1H; implemented by `sys.magnet=18.8`.
- Lines 28-29: Minimal heteronuclear model: one observed 1H and one controlled 14N; implemented by `sys.isotopes={'1H','14N'}`.
- Lines 33-36: Effective NH interaction to be refocused by 14N irradiation. This is a reduced-model surrogate for the residual proton-nitrogen interaction in the decoupling problem, not a literal full solid-state Hamiltonian.; implemented by `inter.coupling.scalar{1,2}=1500`.
- Lines 38-39: Basis set; implemented by `bas.formalism='sphten-liouv'`.
- Lines 42-43: Spinach housekeeping; implemented by `spin_system=create(sys,inter)`.
- Lines 46-49: Observed 1H states used to define the transverse-preservation target. Only transverse magnetisation is used because that is the part directly relevant to proton line preservation during decoupling.; implemented by `Hx=state(spin_system,'Lx','1H'); Hx=Hx/norm(full(Hx),2)`.
- Lines 52-53: 14N control and shift operators; implemented by `LxN=operator(spin_system,'Lx','14N')`.
- Lines 57-58: Drift Hamiltonian; implemented by `H=hamiltonian(assume(spin_system,'nmr'))`.
- Lines 60-69: Paper-inspired practical regime: -800 MHz 1H field -low-power 14N irradiation around 15-23 kHz -10 us pulse elements -a millisecond-scale decoupling block rather than a single very short cycle, because the proton dephasing and Bloch-Siegert accumulation become visible over acquisition-time decoupling windows The offset windows below are intentionally narrower than a full quadrupolar powder/MAS treatment because this is an effective model.; implemented by `rf_nominal_hz=20e3`.
- Lines 77-78: Define the phase-only optimisation problem on 14N with a 1H target; implemented by `control.isotopes={'14N'}`.
- Lines 94-95: Bloch-Siegert correction on the driven 14N channel; implemented by `control.bsiegert=true()`.
- Lines 97-98: Spinach housekeeping; implemented by `spin_system_bs=optimcon(spin_system,control)`.
- Lines 100-104: Optimise a low-power phase-modulated 14N cycle. The seed is an XY-type phase cycle rather than a random guess because the heteronuclear preservation problem is structured; random initial phases can give exactly zero overlap for some ensemble members and make GRAPE abort.; implemented by `guess=reshape((pi/2)*mod(0:nsteps-1,4),1,[])`.
- Lines 107-108: Constant-phase baseline with the same duration and RF power; implemented by `phi_cw=zeros(1,nsteps)`.
- Lines 110-113: Evaluate both waveforms on dense 14N offset and B1 grids; implemented by `[profile_opt,mean_offset_opt]=offset_profile(spin_system_bs,H,{LxN,LyN}, control.pulse_dt,Hx,Hy,LzN,phi_opt,rf_nominal_hz,eval_offsets_hz, eval_b1_scales)`.
- Lines 127-129: Training-set figures of merit; implemented by `train_opt=ensemble_score(spin_system_bs,H,{LxN,LyN},control.pulse_dt, Hx,Hy,LzN,phi_opt,rf_nominal_hz,train_offsets_hz,train_b1_scales)`.
- Lines 134-135: Plot the result; implemented by `figure`.
- Lines 165-166: Secondary robustness figures; implemented by `figure`.

### Key state/data transformations

- Lines 26: computes `sys.magnet` using `sys.magnet=18.8`.
- Lines 29: computes `sys.isotopes` using `sys.isotopes={'1H','14N'}`.
- Lines 30: computes `inter.zeeman.scalar` using `inter.zeeman.scalar={0,0}`.
- Lines 31: computes `inter.coupling.scalar` using `inter.coupling.scalar=cell(2)`.
- Lines 36: computes `inter.coupling.scalar{1,2}` using `inter.coupling.scalar{1,2}=1500`.
- Lines 39: computes `bas.formalism` using `bas.formalism='sphten-liouv'`.
- Lines 40: computes `bas.approximation` using `bas.approximation='none'`.
- Lines 43: computes `spin_system` using `spin_system=create(sys,inter)`.
- Lines 49: computes `Hx` using `Hx=state(spin_system,'Lx','1H'); Hx=Hx/norm(full(Hx),2)`.
- Lines 50: computes `Hy` using `Hy=state(spin_system,'Ly','1H'); Hy=Hy/norm(full(Hy),2)`.
- Lines 53: computes `LxN` using `LxN=operator(spin_system,'Lx','14N')`.
- Lines 54: computes `LyN` using `LyN=operator(spin_system,'Ly','14N')`.
- Lines 55: computes `LzN` using `LzN=operator(spin_system,'Lz','14N')`.
- Lines 58: computes `H` using `H=hamiltonian(assume(spin_system,'nmr'))`.
- Lines 69: computes `rf_nominal_hz` using `rf_nominal_hz=20e3`.
- Lines 70: computes `nsteps` using `nsteps=120`.
- Lines 71: computes `pulse_dt` using `pulse_dt=10e-6*ones(1,nsteps)`.
- Lines 72: computes `train_offsets_hz` using `train_offsets_hz=linspace(-12e3,12e3,7)`.

### Local helper functions

- Line 201: `offset_profile()` — `function [score_map,mean_profile]=offset_profile(spin_system,H,controls,`. Fidelity map across 14N offsets and B1 scales
  - Representative operation: `pulse_dt,Hx,Hy,LzN,phi_profile,rf_nominal_hz,offsets_hz,b1_scales)`.
  - Representative operation: `score_map=zeros(numel(b1_scales),numel(offsets_hz))`.
- Line 216: `b1_profile()` — `function [score_map,mean_profile]=b1_profile(spin_system,H,controls,`. Fidelity map across 14N offsets and B1 scales
  - Representative operation: `pulse_dt,Hx,Hy,LzN,phi_profile,rf_nominal_hz,offsets_hz,b1_scales)`.
  - Representative operation: `score_map=zeros(numel(b1_scales),numel(offsets_hz))`.
- Line 231: `ensemble_score()` — `function score=ensemble_score(spin_system,H,controls,pulse_dt,Hx,Hy,`. Training-ensemble fidelity matrix
  - Representative operation: `LzN,phi_profile,rf_nominal_hz,offsets_hz,b1_scales)`.
  - Representative operation: `score=zeros(numel(b1_scales),numel(offsets_hz))`.
- Line 245: `single_score()` — `function score=single_score(spin_system,H,controls,pulse_dt,Hx,Hy,`. Build the physical 14N waveform
  - Representative operation: `LzN,phi_profile,rf_nominal_hz,offset_hz,b1_scale)`.
  - Representative operation: `amp_profile=2*pi*rf_nominal_hz*b1_scale*ones(size(phi_profile))`.

## Implementation structure

- Reduced heteronuclear control demonstration inspired by the low-power
- offset-tolerant 14N decoupling papers of Nehra, Agarwal, and Nishiyama.
- The model uses an observed 1H spin coupled to a controlled 14N spin:
- -controls act on 14N only,
- -the optimisation target is preservation of 1H magnetisation,
- -14N offset and B1 distributions represent the practical decoupling
- problem,
- -the Bloch-Siegert term is generated by, and applied to, the driven
- 14N channel.
- This is deliberately a reduced effective model rather than a full MAS
- quadrupolar simulation. In particular, the broad 14N quadrupolar/MAS
- response is compressed into an effective 14N offset ensemble, and the

## Internal Spinach / MATLAB structure cues

- Called routines detected from the main body: `rng()`, `create()`, `basis()`, `state()`, `operator()`, `hamiltonian()`, `assume()`, `true()`, `optimcon()`, `fmaxnewton()`, `offset_profile()`, `b1_profile()`, `ensemble_score()`, `cumsum()`, `subplot()`, `unwrap()`.
