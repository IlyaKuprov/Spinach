# interfaces/nmrpipe/fid2ascii.m

- Source: `/home/kuprov/.openclaw/workspace/Spinach/interfaces/nmrpipe/fid2ascii.m`
- Signature: `fid2ascii(file_name,fid)`
- Total lines: 103

## Purpose

Writes out 1D, 2D and 3D free induction decays generated by Spinach as ASCII files in the following format [time1, time2, time3, value]. Time is represented by point numbers, the sweep width should be provided by the user to whatever external program is processing the data. Syntax: fid2ascii(filename,fid)

## Physical / mathematical content

- NMRPipe interfaces. These files write or translate Spinach time-domain and spectral data into formats used by NMRPipe-style processing workflows.

## Numerical / algorithmic content

- The file contains an explicit `grumble(...)` validator, which is Spinach convention for front-loading dimension, type, and regime checks before expensive linear-algebra work begins.
- The file also defines local helper function(s): `grumble()`. This usually means the public entry point is supported by tightly coupled validation or helper logic kept private to the file.

## Code-derived implementation details

### Comment-guided execution stages

- Lines 28-29: Check consistency; implemented by `grumble(fid)`.
- Lines 31-32: Open the file for writing; implemented by `file_id=fopen(file_name,'w')`.
- Lines 34-35: Decide data dimensions; implemented by `if isvector(fid)`.
- Lines 37-38: Interleave real and imaginary parts; implemented by `for n=1:numel(fid)`.
- Lines 47-48: Write out the fid; implemented by `for k=1:size(fid,2)`.
- Lines 50-51: Interleave real and imaginary parts; implemented by `for n=1:size(fid,1)`.
- Lines 62-63: Write out the fid; implemented by `for m=1:size(fid,3)`.
- Lines 79-80: Complain and bomb out; implemented by `error('unsupported data dimensionality.')`.
- Lines 84-85: Close the file; implemented by `fclose(file_id)`.

### Control flow inferred from the code

- Line 35: conditional branch on `isvector(fid)`.
- Line 38: `for` loop over `n=1:numel(fid)`.
- Line 41: `for` loop over `n=1:numel(fid)`.
- Line 48: `for` loop over `k=1:size(fid,2)`.
- Line 51: `for` loop over `n=1:size(fid,1)`.
- Line 54: `for` loop over `n=1:size(fid,1)`.
- Line 63: `for` loop over `m=1:size(fid,3)`.
- Line 64: `for` loop over `k=1:size(fid,2)`.
- Line 67: `for` loop over `n=1:size(fid,1)`.
- Line 70: `for` loop over `n=1:size(fid,1)`.

### Key state/data transformations

- Lines 32: computes `file_id` using `file_id=fopen(file_name,'w')`.

### Local helper functions

- Line 90: `grumble()` — `function grumble(fid)`. To be a -I don't know if this phrase is an oxymoron -but to be a sensible theologian, or at least one who has a pretence of being scholarly, you at
  - Representative operation: `if ~isnumeric(fid), error('fid must be numeric.'); end`.

## Parameters / inputs

- filename -a character string
- fid -a numeric data array (structure arrays
- must be exported field by field)

## Outputs

- this function writes a file
- Note: complex data is exported as a double point count in the first di-
- mension. First the reals are given, then the imags.

## Implementation structure

- Writes out 1D, 2D and 3D free induction decays generated by Spinach as
- ASCII files in the following format [time1, time2, time3, value]. Time
- is represented by point numbers, the sweep width should be provided by
- the user to whatever external program is processing the data. Syntax:
- fid2ascii(filename,fid)
- filename -a character string
- fid -a numeric data array (structure arrays
- must be exported field by field)
- this function writes a file
- Note: complex data is exported as a double point count in the first di-
- mension. First the reals are given, then the imags.
- Check consistency

## Internal Spinach / MATLAB structure cues

- Called routines detected from the main body: `grumble()`, `fopen()`, `isvector()`, `fid()`, `ismatrix()`, `ndims()`, `fclose()`.
