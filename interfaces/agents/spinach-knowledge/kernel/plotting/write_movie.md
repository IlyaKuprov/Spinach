# kernel/plotting/write_movie.m

- Signature: `write_movie(file_name)`

## Purpose

Orbits the camera around a 3D plot and writes a correctly looping 359-frame movie. Intended for generating presenta- tion quality movies from graphics generated by volplot(), cst_display(), hfc_display(), and other Spinach plotting utilities. Syntax: write_movie(file_name)

## Physical / mathematical content

## Numerical / algorithmic content

## Parameters / inputs

- file_name -a character strong with a file name

## Outputs

- this function writes a file

## Implementation structure

- Orbits the camera around a 3D plot and writes a correctly
- looping 359-frame movie. Intended for generating presenta-
- tion quality movies from graphics generated by volplot(),
- cst_display(), hfc_display(), and other Spinach plotting
- utilities. Syntax:
- write_movie(file_name)
- file_name -a character strong with a file name
- this function writes a file
- Check consistency
- Disable stretch-to-fill
- Open the video writer object
- Orbit the camera
