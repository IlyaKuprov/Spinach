# tests/kernel/test_liouville_commutator.m

- Source: `/home/kuprov/.openclaw/workspace/Spinach/tests/kernel/test_liouville_commutator.m`
- Signature: `result=test_liouville_commutator()`
- Total lines: 42

## Purpose

Tests Liouville-space commutation superoperators. Syntax: result=test_liouville_commutator()

## Physical / mathematical content

- This file belongs to the `tests` part of Spinach. Its role should be read together with nearby files in the same directory, which usually share a common physical regime or infrastructure purpose.

## Numerical / algorithmic content

- The file is built around the standard Spinach workflow: create the spin system, choose a basis or context, assemble operators/superoperators, then propagate or analyse the resulting dynamics.

## Code-derived implementation details

### Comment-guided execution stages

- Lines 16-17: Announce the test target; implemented by `fprintf('TESTING: Liouville commutation superoperator\n')`.
- Lines 19-22: State the Liouville-space target of the test; implemented by `result=new_test_result('kernel/liouville_commutator', 'Liouville commutation superoperator', 'commutation superoperators must be left multiplication minus right multipli…`.
- Lines 24-25: Build a one-proton Zeeman Liouville-space spin system; implemented by `sys.magnet=0`.
- Lines 32-33: Generate three forms of the same labelled operator; implemented by `L_comm=operator(spin_system,'Lz',1,'comm')`.
- Lines 37-39: Check the superoperator identity; implemented by `result=test_close(result,'comm=left-right',L_comm,L_left-L_right,1e-15,1e-15, 'the Liouville commutator is A*rho-rho*A')`.

### Key state/data transformations

- Lines 20-22: computes `result` using `result=new_test_result('kernel/liouville_commutator', 'Liouville commutation superoperator', 'commutation superoperators must be left multiplication minus right multipli…`.
- Lines 25: computes `sys.magnet` using `sys.magnet=0`.
- Lines 26: computes `sys.isotopes` using `sys.isotopes={'1H'}`.
- Lines 27: computes `inter.zeeman.scalar` using `inter.zeeman.scalar={0}`.
- Lines 28: computes `bas.formalism` using `bas.formalism='zeeman-liouv'`.
- Lines 29: computes `bas.approximation` using `bas.approximation='none'`.
- Lines 30: computes `spin_system` using `spin_system=test_spin_system(sys,inter,bas)`.
- Lines 33: computes `L_comm` using `L_comm=operator(spin_system,'Lz',1,'comm')`.
- Lines 34: computes `L_left` using `L_left=operator(spin_system,'Lz',1,'left')`.
- Lines 35: computes `L_right` using `L_right=operator(spin_system,'Lz',1,'right')`.

## Outputs

- result -regression test result with explanatory messages
- The test checks that the commutation superoperator generated by operator()
- is equal to the difference between left and right product superoperators.

## Implementation structure

- Tests Liouville-space commutation superoperators. Syntax:
- result=test_liouville_commutator()
- result -regression test result with explanatory messages
- The test checks that the commutation superoperator generated by operator()
- is equal to the difference between left and right product superoperators.
- Announce the test target
- State the Liouville-space target of the test
- Build a one-proton Zeeman Liouville-space spin system
- Generate three forms of the same labelled operator
- Check the superoperator identity

## Internal Spinach / MATLAB structure cues

- Called routines detected from the main body: `new_test_result()`, `test_spin_system()`, `operator()`, `test_close()`.
